import React from 'react';
import { CenterStage } from '../styles/BaseStyling';

export default class HomeView extends React.Component {
  render() {
    return (
      <CenterStage>
        Everything is going to be ok.
      </CenterStage>
    );
  };
};